on: push
env:
  CI: true
defaults:
  runs-on: ubuntu-latest
  steps:
  - uses: actions/checkout@v2
  - uses: actions/setup-node@v1
    with:
      node-version: '12.x'
  - run: npm ci
jobs:
  coverage:
    run: npm run coverage
  lint:
    run: npm run lint
